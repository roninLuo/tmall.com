{
  "name": "www.tmall.com",
  "version": "1.0.0",
  "description": "bash \r <!-- 每台计算机只需要执行一次 -->\r $ git config --global user.name '***'\r $ git config --global user.email '***'\r <!-- 已执行 -->",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
